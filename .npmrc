# Environment configuration for package installation
# This file sets environment variables for npm operations

# Skip Puppeteer Chrome download during package installation
# Puppeteer is used only for local development tools (PDF generation)
# Tests use mock stores and don't require actual browser automation
puppeteer_skip_download=true